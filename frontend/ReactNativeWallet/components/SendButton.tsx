/* eslint-disable react-native/no-inline-styles */
import React from 'react';
import {Text, View} from 'react-native';
import {Colors, Fonts} from '../screens/style';

const TransferButton = () => {
  return (
    <View style={{}}>
    </View>
  );
};

export default TransferButton;
